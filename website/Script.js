const inviteButton = document.getElementById("invite-button");

inviteButton.addEventListener("click", function (event) {
    event.preventDefault();

    alert(
        "O link de convite do Califórnia ainda não foi configurado."
    );
});
